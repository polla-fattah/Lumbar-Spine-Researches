% ===========================================================================
%  THESIS WRAPPER -- master document
%
%  Automated Classification of Lumbar Degenerative Disease from MRI
%  PhD candidature, Rizgary Teaching Hospital lumbar spine research programme
%
%  Build:   pdflatex thesis
%           biber --output-safechars thesis
%           pdflatex thesis ; pdflatex thesis
%
%  The --output-safechars flag is REQUIRED, not cosmetic. Biber decodes LaTeX
%  accent macros from the .bib into Unicode, and does so in DECOMPOSED form:
%  Ancu{\c{t}}i becomes 't' followed by a standalone combining cedilla
%  (U+0327), which inputenc cannot typeset. The flag converts non-ASCII back
%  into LaTeX macros, giving a pure-ASCII .bbl. Without it the build fails on
%  any cited author with an accented name.
%
%  ---------------------------------------------------------------------------
%  WHY THIS FILE EXISTS
%
%  chapter2.tex and chapter3.tex are each complete standalone documents, with
%  their own \documentclass, preamble and \end{document}. That is deliberate and
%  worth keeping: a chapter can be compiled and sent to a supervisor on its own
%  in a couple of seconds without building the whole thesis.
%
%  The cost of standalone chapters is that cross-chapter references cannot
%  resolve. Chapter 3 refers to \ref{ch:litreview} five times; compiled alone
%  those emit "Chapter ??". Only a single document containing both chapters can
%  resolve them, and only such a build can confirm that the gap -> research
%  question -> contribution numbering holds end to end.
%
%  ---------------------------------------------------------------------------
%  HOW IT WORKS -- and why the chapter files were NOT modified
%
%  The docmute package makes \input ignore everything in the included file up to
%  \begin{document} and after \end{document}. The chapter preambles are
%  therefore skipped automatically at build time.
%
%  This means chapter2.tex and chapter3.tex are byte-for-byte unchanged and keep
%  compiling standalone exactly as before. There are no duplicated bodies to
%  keep in sync, which is the usual failure mode of the alternative approach
%  (splitting each chapter into a preamble file plus a body file).
%
%  The consequence is that every package a chapter needs must be declared HERE,
%  because the \usepackage lines in the chapters are ignored. The preamble below
%  is the union of both chapter preambles. If you add a package to a chapter,
%  add it here too or the thesis build will fail while the chapter still works.
% ===========================================================================

\documentclass[12pt,a4paper]{report}

% --- union of the chapter preambles -----------------------------------------
\usepackage{lmodern}          % scalable fonts; required for microtype expansion
\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage[english]{babel}
\usepackage{csquotes}      % loaded by chapter1; also recommended by biblatex
\usepackage{microtype}
\usepackage[margin=2.5cm]{geometry}
\usepackage{setspace}
\onehalfspacing

\usepackage{amsmath,amssymb}
\usepackage{booktabs}
\usepackage{longtable}
\usepackage{array}
\usepackage{graphicx}
\usepackage{thesisstyle}   % shared TikZ figure styles; see thesisstyle.sty
\usepackage[hidelinks]{hyperref}

\usepackage[
  backend=biber,
  style=numeric-comp,
  sorting=none,
  maxbibnames=6,
  minbibnames=3,
  giveninits=true,
  doi=true,
  url=false,
  isbn=false
]{biblatex}

% Single bibliographic source for the whole thesis: 149 records, comprising the
% 92 curated lumbar-spine papers, the 16 foundational method/clinical papers,
% and the methodology references filed as records 111-149.
\addbibresource{../lumbar_spine_mri_ai_literature_inventory.bib}

% The inventory .bib carries internal bookkeeping fields (provenance note, the
% per-paper summary, the local PDF path). These are working metadata, not
% bibliographic data, so they are suppressed from the printed reference list.
% Leaving the note field in also breaks compilation, since it contains raw
% underscores that LaTeX reads as math subscripts.
\AtEveryBibitem{%
  \clearfield{note}%
  \clearfield{comment}%
  \clearfield{annotation}%
  \clearfield{abstract}%
  \clearfield{file}%
}

% Prospective-protocol markers, defined in chapter3.tex. Chapter 3 is written
% before the experiment is run; these mark what must be replaced with the
% executed protocol rather than filled in from intention.
\newcommand{\toconfirm}[1]{\textbf{[TO CONFIRM: #1]}}
\newcommand{\torecord}[1]{\textbf{[TO RECORD AFTER IMPLEMENTATION: #1]}}
\newcommand{\laterinsert}[1]{\textbf{[LATER INTEGRATION: #1]}}

% --- machinery for folding standalone chapters in ---------------------------
\usepackage{docmute}          % \input skips each child's preamble

% Each chapter ends with its own \printbibliography, which is correct when it is
% compiled alone but would produce a reference list after every chapter here.
% Swallow those calls (including their optional argument) and emit one
% consolidated bibliography at the end instead.
\newcommand{\thesisgobblebib}[1][]{}
\let\thesisrealbibliography\printbibliography
\let\printbibliography\thesisgobblebib

% --- title page fields ------------------------------------------------------
% Placeholders are marked so they cannot be mistaken for settled facts.
\newcommand{\thesistitle}{Disease-Adaptive Heterogeneous Graph Learning with
  Anatomically Aligned Multi-Sequence MRI Representations for Lumbar
  Degenerative Disease}
\newcommand{\thesiscandidate}{\toconfirm{candidate's full name as registered}}
\newcommand{\thesissupervisor}{Dr.\ Polla Fattah}
\newcommand{\thesisinstitution}{\toconfirm{awarding institution and department}}
\newcommand{\thesisdegree}{\toconfirm{degree for which the thesis is submitted}}

\begin{document}

% ===========================================================================
%  FRONT MATTER
% ===========================================================================
\pagenumbering{roman}

\begin{titlepage}
\centering
\vspace*{2cm}

{\Large\bfseries \thesistitle \par}

\vspace{2.5cm}
{\large \thesiscandidate \par}

\vspace{1cm}
{\normalsize Supervised by \thesissupervisor \par}

\vspace{2.5cm}
{\normalsize \thesisinstitution \par}
\vspace{0.5cm}
{\normalsize \thesisdegree \par}

\vfill

\begin{minipage}{0.85\textwidth}
\footnotesize\raggedright
\textbf{Status of this build.} This document contains Chapters 1 to 5:
introduction, literature review, methodology, results, and discussion and
conclusions. Chapters are numbered naturally; the counter advance used while
Chapter~1 was unwritten has been removed. Research question RQ4,
cross-institutional transfer, was not executed and is reported as such rather
than omitted. Data collection is conducted at Rizgary Teaching Hospital; no
patient-identifiable data appears in this document or in the repository that
produces it.
\end{minipage}

\vspace{1cm}
{\footnotesize Compiled \today \par}
\end{titlepage}

\cleardoublepage
\chapter*{Abstract}
\addcontentsline{toc}{chapter}{Abstract}

Magnetic resonance grading of lumbar degenerative disease is slow, and expert
readers agree with one another only moderately: reported inter-reader $\kappa$
ranges from $0.49$ for subarticular stenosis to $0.73$ for the central canal.
This thesis asks whether explicit anatomical structure, imposed on a
multi-sequence convolutional model, improves automated grading on the LumbarDISC
benchmark of 1{,}974 studies and 48{,}657 annotated targets.

Three mechanisms were proposed and each was tested against a control matched in
capacity rather than against its own absence: anatomically aligned cross-sequence
self-supervision, disease-conditioned sequence routing, and a heterogeneous
disease--anatomy graph. An eight-rung ablation ladder was executed over seven
training seeds, seventy runs in total, with inference on a frozen patient-level
test partition and false discovery rate control across a pre-specified
comparison family.

The complete system improves on a single-sequence baseline by $+0.0177$
quadratic weighted kappa (95\% CI $[+0.0097, +0.0260]$) on every seed, and
Severe$\rightarrow$Normal confusions fall from $5.7\%$ to $3.8\%$. Two further
comparisons survive correction: typed heterogeneous graph edges
($+0.0093$, $p_{\mathrm{FDR}} = 0.009$) and a clinically cost-sensitive ordinal
objective ($+0.0082$, $p_{\mathrm{FDR}} = 0.009$), the latter carrying the
largest standardised effect in the study.

The anatomical mechanisms do not. Cross-sequence self-supervision confers no
measurable benefit on accuracy, on robustness to a withheld sequence, or on
where the model attends. Disease-conditioned routing reproduces the clinically
expected sequence preference in every run, yet a router-free model shows the
same preference under intervention, identifying the pattern as a property of the
benchmark's annotation structure rather than of routing. Anatomically correct
graph topology does not outperform a degree-preserving random shuffle. These are
bounds rather than absences: the paired intervals constrain the three mechanisms
to at most $1.22\%$, $1.55\%$ and $1.05\%$ of baseline performance.

Attribution analysis supplies a mechanism for that pattern. A plain
single-sequence encoder already concentrates $2.4$ times the chance share of its
evidence on the annotated lesion, so the localisation these priors were
introduced to supply is largely accomplished before any of them is added.

The contributions are therefore a verified multi-sequence grading system, a
controlled ablation protocol in which three analyses that initially produced
positive conclusions were overturned by their own controls, and evidence that
anatomical structural priors do not pay at this data scale together with a
measured explanation of why.

\vspace{0.5em}
\noindent\textbf{Keywords:} lumbar spinal stenosis, magnetic resonance imaging,
ordinal classification, graph neural networks, self-supervised learning,
ablation study, negative results.

\tableofcontents

% ===========================================================================
%  MAIN MATTER
% ===========================================================================
\cleardoublepage
\pagenumbering{arabic}

% All five chapters exist and are numbered naturally. The \setcounter hack
% that formerly advanced the chapter counter, from when Chapter 1 was unwritten,
% has been removed -- leaving it in would number the introduction as 2 and shift
% every later chapter and cross-reference by one.
\input{chapter1}

\input{chapter2}

\input{chapter3}

\input{chapter4}      % Results
\input{chapter5}      % Discussion and Conclusions

% Chapter 5 covers both discussion and conclusions, so no separate chapter 6 is
% planned. Uncomment below only if they are later split.
%
% \input{chapter6}    % Conclusions

% ===========================================================================
%  BACK MATTER
% ===========================================================================
\cleardoublepage
\let\printbibliography\thesisrealbibliography
\printbibliography[heading=bibintoc,title={References}]

\end{document}
